# Set default behaviour to automatically normalize line endings
* text=auto

# Force batch scripts (Windows) to always use CRLF line endings
*.{cmd,[cC][mM][dD]} text eol=crlf
*.{bat,[bB][aA][tT]} text eol=crlf

# Force POSIX shell scripts (macOS) to always use LF line endings
*.[z]sh text eol=lf
